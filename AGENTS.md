## Agent skills

- Context: in-repo domain docs and ADRs. Read `docs/agents/domain.md`, then `CONTEXT-MAP.md` / package `CONTEXT.md` files as directed. ADRs live under `docs/adr/` and package `adr/` directories.
- Issues: local markdown files under `.scratch/<feature-slug>/`; see `docs/agents/issue-tracker.md`.
- Triage: off; no issue label/status convention is required.
- Layout: multi-context (`CONTEXT-MAP.md` at the repo root points to package contexts).
