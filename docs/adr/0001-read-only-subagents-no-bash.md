# Keep Read-Only Subagents Off Bash

Read-only `plan` + `explore` subagents get no `bash` in MVP, even for search. Dedicated read/search/list tools enforce read-only boundary. Shell allowlist revisitable later if Pi SDK exposes safe wrapper.
