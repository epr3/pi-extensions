{
  "name": "@epr3/pi-extension-question",
  "version": "0.1.0",
  "private": true,
  "description": "Structured multiple-choice `question` tool (Pi has none built in).",
  "type": "module",
  "main": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --root ."
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "catalog:",
    "@earendil-works/pi-tui": "catalog:",
    "typebox": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}