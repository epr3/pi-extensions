{
  "name": "@epr3/pi-extension-subagents",
  "version": "0.1.0",
  "private": true,
  "description": "`Agent` + `get_subagent_result` tools. Types: explore (read-only), general (scoped read/write).",
  "type": "module",
  "main": "./index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --root ."
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.80.2",
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "@earendil-works/pi-tui": "^0.80.2",
    "typebox": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}